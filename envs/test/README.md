# Test Environment

Test Terraform roots belong in the component directories below. Copy and adapt production configuration only after test tenant, subscription, state backend, and principal IDs are approved.
